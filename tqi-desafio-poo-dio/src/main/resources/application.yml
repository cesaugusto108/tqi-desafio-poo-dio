server:
  port: 8080
  servlet:
    context-path:

spring:
  web:
    resources:
      add-mappings: false
  main:
    banner-mode: off
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://
    username:
    password:
  jpa:
    hibernate:
      ddl-auto: none
  sql:
    init:
      mode: never